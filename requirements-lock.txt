# Pinned versions for bit-exact reproduction (the set used to produce the
# results). requirements.txt holds the supported floors; use this file for
# an exact match: pip install -r requirements-lock.txt
numpy==2.2.6
sympy==1.14.0
mpmath==1.3.0
